<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <title><%= title %></title>
  </head>
  <body>
    <h1>About Page</h1>
    <% let items = ["Apple", "Grapes", "Banana"] %>

    <ul>
      <% items.forEach((i) => { %>
      <li><%= i _%></li>
      <% }) %>
    </ul>
  </body>
</html>
